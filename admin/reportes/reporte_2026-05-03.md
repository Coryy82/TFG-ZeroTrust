# Reporte Ejecutivo - 2026-05-03

## KPIs clave
- **Commits últimos 7 días:** 6 entradas de log: 5 commits con aportación directa y 1 merge.
- **Velocidad semanal:** +500% frente a la semana anterior si se cuenta el merge (1 -> 6); +400% en commits con cambios propios (1 -> 5).
- **Commits técnicos verificables:** 1 (`infra/perimetral` + diario de laboratorio).
- **Entregables críticos Escenario A cerrados:** 1/4 parcialmente cerrado: estructura Docker Compose creada, pero entorno no ejecutable.
- **Riesgos prioritarios activos:** 4.

## Lectura ejecutiva
La semana muestra recuperación de actividad tras una fase de baja velocidad. Se creó la estructura del laboratorio perimetral con `docker-compose.yaml`, redes `net_dmz` y `net_interna`, servicios `nginx`, `webapp`, `backend` y `db`, además de documentación de diario y ajustes de supervisión académica.

El avance mejora la trazabilidad técnica, pero no cierra aún la baseline del Escenario A. Persisten ficheros críticos vacíos (`app.py`, `Dockerfile`, `requirements.txt`, `nginx.conf`, `init.sql`) y el propio diario confirma que la red perimetral todavía no levanta.

## Riesgos
1. **Retraso de roadmap:** Mayo exige Zero Trust + Wazuh, pero el Escenario A sigue sin validación.
2. **Riesgo metodológico:** no hay evidencias reproducibles de conectividad ni ataque.
3. **Riesgo académico:** el Estado del Arte continúa pendiente.
4. **Riesgo técnico:** `depends_on` y healthcheck pueden fallar hasta completar backend e imagen base.

## Prioridades próximas
1. Completar servicios `webapp`, `backend`, `nginx` y `db`.
2. Ejecutar `docker compose up --build` y guardar evidencias.
3. Documentar flujo de ataque perimetral.
4. Iniciar investigación mínima de Wazuh sin abandonar la baseline A.
